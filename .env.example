# Frontend Environment Variables
# Copy this file to .env.local for local development
# For Vercel deployment, set these in Project Settings > Environment Variables

# API Configuration - REQUIRED for all environments
# This tells the frontend where to find the backend API
# 
# MONOREPO DEPLOYMENT (recommended):
# - Vercel deployment: VITE_API_URL=/api
# - Local development: VITE_API_URL=/api (Vite proxy forwards to http://localhost:3000)
# - Both frontend and backend deployed together in single Vercel project
# - Backend runs as serverless function in /api directory
#
# SEPARATE DEPLOYMENT (alternative):
# - Vercel deployment: VITE_API_URL=https://your-backend.vercel.app/api
# - Local development: VITE_API_URL=http://localhost:3000/api
# - Frontend and backend are separate Vercel projects
# - Requires CORS configuration on backend
#
VITE_API_URL=/api

# Backend Environment Variables
# Set these in Vercel project environment variables

# Database Configuration - NeonDB (PostgreSQL)
# These are typically set automatically by Neon integration
LA_DATABASE_URL=postgresql://user:password@host/database
LA_POSTGRES_URL=postgresql://user:password@host/database
LA_POSTGRES_PRISMA_URL=postgresql://user:password@host/database?schema=public
LA_POSTGRES_URL_NO_SSL=postgresql://user:password@host/database

# Node Environment
NODE_ENV=production

# Development only
# These should NOT be set in production Vercel deployment
# PORT=3000
# LOG_LEVEL=debug
