# API Configuration
# For development: Use relative path '/api' to leverage Vite proxy (recommended)
# For production: Set to full API URL (e.g., https://api.legendsascend.com/api)
VITE_API_URL=/api

# Landing Page Configuration
VITE_LANDING_PAGE_ENABLED=true
